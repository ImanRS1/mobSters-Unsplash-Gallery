import React from 'react';
import PaginationButton from './PaginationButton';

const Pagination = () => {
  return (
    <div>
      <PaginationButton />
      <PaginationButton />
    </div>
  )
}

export default Pagination
