import React from 'react';
import styled from 'styled-components';

const SearchText = () => {
  return (
    <Text>Type something and press search to get images!</Text>
  )
}

const Text = styled.p`
  font-size: 2rem;
  text-align: center;
  padding: 2rem 1rem;
`;

export default SearchText
